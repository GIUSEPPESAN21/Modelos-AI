seed: 42

data:
  val_ratio: 0.15
  test_ratio: 0.15
  treatment_col: health_policy_treatment
  outcome_col: outcome_life_expectancy

model:
  backend: auto  # auto | econml | doubleml | manual
  n_folds: 5

paths:
  model_dir: results/trained_models
  metrics_dir: results/metrics
  viz_dir: results/visualizations
