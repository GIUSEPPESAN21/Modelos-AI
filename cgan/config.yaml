seed: 42

data:
  val_ratio: 0.15
  test_ratio: 0.15

model:
  noise_dim: 32
  hidden_dim: 128

training:
  epochs: 50
  batch_size: 64
  lr_g: 0.0002
  lr_d: 0.0002

paths:
  model_dir: results/trained_models
  metrics_dir: results/metrics
  viz_dir: results/visualizations
